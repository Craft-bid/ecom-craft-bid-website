import { Card, Grid } from '@mui/material';

export function SubmitOfferForm() {
  const formSxObj = { backgroundColor: '#F5FBFB', width: '55rem', minHeight: 1400, borderRadius: 10 };

  return <Card sx={formSxObj}></Card>;
}
